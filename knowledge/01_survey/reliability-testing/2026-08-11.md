# 可靠性测试追踪 (2026-08-11)

> **执行时间**: 2026-08-11 22:30 CST
> **任务**: 可靠性测试追踪
> **Token预算**: ≤20K · **输出**: ≤15KB

---

## 执行摘要

- 3 源计划：arXiv cs.DC recent ✅（**Tue, 11 Aug 2026 批次 44 条新提交**，编号 2608.07502~09748）、arXiv 搜索 fault tolerance ✅（30 条，最新 submitted 08-09 Orcaella v2 已在册）、arXiv 搜索 GPU fault ✅（仅 1 条 2605.26461 MPS 已在 08-06 在册跳过）
- **✅ 昨日预期修正验证成功**：北京 08-11（周二）当天已可见 Tue 批次——「北京周二当天可追踪 Tue 批次，无需等到次日 08:00」规律实证成立（连续两日验证：Mon 批 08-10 当天可见、Tue 批 08-11 当天可见）
- 查重：Mon 批次 17 条昨日已全量处理；Tue 批次 44 条筛选出 4 条相关（FlashBoot/eIRWR/OasisKV/ElastiCo），编号均未在册
- **新增 4 条**（强相关 2 + 中相关 2），URL 逐条 curl 验证 HTTP 200 ✅

## 当日信号

- **FlashBoot: 机架级大模型亚秒级权重加载**（arXiv:2608.08482, 08-11）: 面向大模型机架级快速启动的权重加载系统——目标 sub-second 级权重装载，直击 checkpoint 恢复/故障切换的启动瓶颈（训练中断后重新加载权重通常分钟级，FlashBoot 压缩到亚秒）。**与 08-05 收录的 Resume 契约、08-06 FT-HSDP 恢复时间（10min→3min）形成「恢复时间下探」主线**：恢复时延的量级竞争从分钟走向秒级，权重加载成为 checkpoint 恢复路径上的关键瓶颈环节。https://arxiv.org/abs/2608.08482
- **eIRWR: 微服务可扩展根因分析（随机游走重启增强）**（arXiv:2608.08073, 08-11）: Enhanced Iterative Random Walk with Restart——面向大规模微服务的根因分析（RCA）方法，用增强型重启随机游走在服务依赖图中定位故障根因。与 08-10 收录的 eMicro（eBPF 微服务访问控制）同属微服务可观测/诊断线，RCA 是「故障定位→根因→恢复」链路的核心环节；对超节点集群控制面/推理服务的故障诊断有方法学参考。https://arxiv.org/abs/2608.08073
- **OasisKV: 超越 HBM 的 Decode 阶段 KV 缓存（Lookahead 稀疏预取）**（arXiv:2608.08097, 08-11）: 用 lookahead 稀疏预取把 in-decode KV 缓存扩展到 HBM 之外（类似 SSD/远端分层），缓解长上下文解码的显存墙。**与 08-10 收录的 HiSparse（层级 KV 管理）同日同线**——KV 缓存分层迁移成为 LLM 推理基础设施的活跃主题；对 KV 容错边界（KV 四层命运论）而言，KV 分层迁移策略直接决定故障时的状态恢复路径。https://arxiv.org/abs/2608.08097
- **ElastiCo: GPU 集群弹性配置与干扰感知编排**（arXiv:2608.07971, 08-11）: 弹性配置+干扰感知的 GPU 集群编排——在弹性扩缩容的同时感知工作负载间干扰（shared GPU 场景的可靠性与性能隔离）。27 页 9 图 9 表，含 Renyu Yang 等（北航系）。与 08-06 MPS 故障隔离（共享 GPU 场景）互补：MPS 解决故障隔离、ElastiCo 解决干扰隔离，**共享 GPU 资源池的可靠性隔离正从「故障域」扩展到「性能域」**。https://arxiv.org/abs/2608.07971

## 当日趋势

- **恢复时延竞争进入「秒级」赛道**：FlashBoot（亚秒权重加载）承接 FT-HSDP（分钟级恢复）、Concordia（JIT checkpoint）——训练/推理恢复路径的瓶颈依次被拆解：节点替换（FT-HSDP）→ 状态快照（Concordia）→ 权重装载（FlashBoot），**恢复时延的每个环节都在被单独优化**
- **KV 缓存分层迁移成为主旋律**：OasisKV（今日）+ HiSparse（昨日）同日同线——KV 从「单层 HBM 驻留」走向「HBM+远端分层+预取」，分层策略同时决定性能与容错边界（与 KV 四层命运论互证）
- **共享资源池的隔离语义扩展**：MPS（故障隔离）→ ElastiCo（干扰隔离）→ eIRWR（根因定位）——GPU 共享场景的可靠性管理从「防崩溃」走向「防干扰+快定位」的完整谱系

---

## 📊 追踪状态

- **上次有产出日**: 2026-08-10（4+1 条）→ 2026-08-11（本次 4 条），连续有产出
- **连续零产出**: 无（08-08/08-09 占位后已连续 2 日产出）
- **下轮预期**: 北京 08-12（周三）当天可追踪 Wed 批次（规律已连续两日验证：公告批次在北京当天已可见，无需等待次日 08:00）
