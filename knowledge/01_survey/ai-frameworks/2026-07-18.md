# AI框架动态跟踪报告 2026-07-18

> 跟踪范围：arXiv(cs.LG, cs.AR) · vLLM · TensorRT-LLM · SGLang · PyTorch · DeepSpeed · Megatron · Triton · ROCm | 覆盖：推理/训练/编译器/KV Cache
>
> 本期日期：2026-07-18 | 数据截止：2026-07-18 02:45 UTC+8

---

## 本期摘要

- **PyTorch 2.13.0 正式发布**（Jul 8）：CuTeDSL Native DSL 原型、nn.LinearCrossEntropyLoss **4× 显存节省**、FlexAttention MPS **~12× speedup**、torchcomms 新通信后端、FSDP2 重叠通信 + 1000+ 修复
- **SGLang v0.5.15**（Jul 10）：Spec V2 默认（+11% TPS 零开销）、IndexShare MTP（**1.9×** 降 draft-step 成本）、DSV4 FlashMLA 稀疏 prefill **>10%**、Decode Context Parallelism 首落地
- **TRT-LLM v1.3.0rc21**（Jul 15）：DSV4 稀疏 MLA 注意力、Cascade Attention **4.6× vs MMHA**、DeepGEMM/MegaMoE 集成、AutoDeploy 后端废弃公告
- **PEEK 预测性 KV 调度**（arXiv Jul 10）：radix tree 感知待处理队列 → SGLang/vLLM **7.9×/7.1× TTFT**、**6.7×/5.5× E2E**、**3.6×/4.5× throughput**
- **SAC CXL 解耦 KV Cache**（arXiv Jun 17）：CXL 缓存行级按需取 top-k → RDMA baseline **2.1× throughput、9.7× TTFT**
- **STAR-KV 自适应低秩压缩**（arXiv Jun 6）：端到端 **75% 压缩** + 量化叠加 **20×**、Triton kernel **6.9× attention speedup**
- **OScaR INT2 KV 量化**（arXiv May 19）：Canalized Rotation + Omni-Token Scaling 消除 TNI → **3.0× decode speedup、5.3× 显存下降、4.1× throughput**
- **PolyQ CPU 分数比特量化**（ICCAD'26, arXiv Jul 17）：activation-aware 混合位宽 {2,3,4,8,16} → PPL **2.4–32.1%** 改善，重排开销降低 **70.8%**
- **ExaGEMM CPU 低比特 GEMM 探索框架**（ICCAD'26, arXiv Jul 17）：13.29× latency 加速，候选空间剪枝 **99.2%**
- **HMA-Serve 跨厂商解耦推理**（arXiv Jun 29）：GDDR 预填充 + HBM 解码 → **3.2× goodput、4.8× goodput-per-dollar**
- **SwiftCache 跨模型 KV 共享**（arXiv Jun 14）：NVLink 空闲内存 → **P99 TTFT -69%、最大上下文 +3.98×**
- **LUMEN 分布式推理故障恢复**（arXiv Jun 16）：负载感知三点恢复决策 + 模型重载期间容量恢复

---

## 一、框架正式发布 / RC

### PyTorch 2.13.0（Jul 8，正式版）

> https://github.com/pytorch/pytorch/releases/tag/v2.13.0

**1500+ commits，5 大亮点 + 1000+ 修复**

| 亮点 | 说明 | 性能数据 |
|:-----|:-----|:---------|
| **CuTeDSL Native DSL 后端** [Prototype] | Inductor 获得 Triton 之外的第二条高性能 GPU 代码路径 | **编译更快**（相对 Triton） |
| **nn.LinearCrossEntropyLoss** | 融合最终预测层与 loss 计算，消除分类头显存峰值 | 大词表 LM 训练**最高 4× 显存节省** |
| **FlexAttention on MPS** | 稀疏 attention 模式在 Apple Silicon 上原生 | **~12× speedup over SDPA** 在稀疏模式；CUDA 确定性 backward |
| **torchcomms** | 分布式新通信后端 | 改进容错性、可扩展性、可调试性 |
| **FSDP2 通信重叠** | 通过独立进程组重叠 reduce-scatter/all-gather | 提升分布式训练吞吐 |

**关键新特性**：
- Inductor：CuTeDSL grouped GEMM 修复、`_FastCudaLauncher` 向量化 C 扩展减少每次 launch 开销、PadMM AutoHeuristics 默认开启（确定性推理+14% 在 HuggingFace）、添加 128×256×64（stages=4）matmul 配置（大 Hopper 形状 ~1.3×）、NVLink 集合用 CopyEngine 替换消除 SM 争用
- NCCL 分析模型重校准 → 通信计算重叠 **+14% MFU**
- 分布式：`TCPStore` multiGet 减少网络往返 `2*(world_size-1)` → `1`
- 编译优化：指针内存池减少 `cudaHostAlloc` 开销
- AOTI：并行 tensor-to-bytes 序列化 + 共享模型加载避免重复解压
- **最小 NCCL 版本升至 2.23**、移除 Bazel 构建、要求 C++20

---

### SGLang v0.5.15（Jul 10）

> https://github.com/sgl-project/sglang/releases/tag/v0.5.15

**核心性能提升**：

| 特性 | 数据 |
|:-----|:-----|
| **Spec V2 默认**：CUDA-graphable DSA draft-extend，移除 D2H/H2D 同步，融合 metadata ops | **+11% E2E TPS** |
| **IndexShare MTP**：跨 draft steps 复用 indexer top-k | 长上下文**最高 1.9× 降 draft-step 成本** |
| **TopK V2**：融合 top-k 选择与 page-table 变换，运行时 k ≤ 2048 | — |
| **Indexer prologue fusion**：12 kernel → 4 | ~**8%** decode 加速（bs=1） |
| **GEMM**：shape-specialized JIT router GEMM + CuteDSL BF16 GEMM for Blackwell | — |
| **FlashInfer autotune** 覆盖 draft-model 图 | — |

**DSV4 专项优化**：
- C128 state-pool allocation 优化
- **FlashMLA 稀疏 prefill 默认开启** → 长上下文**>10%** throughput gain
- 非分页 indexer 支持长上下文 prefill → **>5%** E2E throughput

**新能力**：
- **Decode Context Parallelism** 首落地（MLA 模型，DSV3/Kimi K2）
- Breakable CUDA Graph 默认（+ 预填充阶段 experimental full CUDA Graph）
- **KDA/GDN 线性 attention kernel**（FlashKDA prefill + ReplaySSM decode）
- GLM-5.2 NVFP4 **500+ tok/s/user on 8× B300**, **450 on 4× GB300** (bs=1)
- 新模型：Hunyuan 3、LocateAnything-3B、Baidu Unlimited-OCR、JoyEcho、Qwen3.6 NVFP4
- FlashInfer all-to-all for routed MoE

---

### TRT-LLM v1.3.0rc21（Jul 15）

> https://github.com/NVIDIA/TensorRT-LLM/releases/tag/v1.3.0rc21

**核心亮点**：

| 特性 | 数据 |
|:-----|:-----|
| **DSV4 稀疏 MLA 注意力后端** | 首批官方 DSV4 支持 |
| **Cascade Attention** | **最高 4.6×** vs 标准 MMHA |
| DeepGEMM / MegaMoE 集成 | DSV4 优化 |
| EAGLE3 动态树 (Blackwell) | — |
| **AutoDeploy 后端废弃公告** | 转向 agentic 方法加速 PyTorch 后端模型支持（已用于 MiniMax M3 首周内） |

**新模型支持**：DeepSeek V4、Cosmos3、MiniMax M3 MXFP8/NVFP4、Gemma 4 12B Unified、Qwen3.5-VL、Qwen3.6 NVFP4、Qwen3.5 MoE

**关键特性**：
- Inflight weight update、Dynamic speculation 扩展到所有 spec decode 算法
- PyTorch encoder-decoder CUDA graph + TP 支持
- Piecewise CUDA Graph for Qwen3.5/3.6 MoE
- 低延迟 host task dispatch for guided decoding
- Cute DSL GVR Top-K short-row optimization（移除 `run_one_row` 的 cluster sync）
- 原生 `/v1/embeddings` dynamic batching for encoder-only
- KV Cache Manager v2 自动选择（per-model）
- Multi-rank sleep/wakeup（MPI executor）
- 代理 fast-death 检测 + sticky EngineDeadError

---

## 二、arXiv 新论文精选（2026-07-17 新列表 + 近期）

### 🥇 PEEK: Predictive Queue-Informed KV Cache Management（cs.DC, May→Jul 10）

> arXiv:2607.02525 · SGLang + vLLM 实现

**核心创新**：在 LLM serving 引擎的 pending queue 之上维护增量 radix tree，暴露前缀共享簇。双遍历匹配 + 协同驱逐。

**性能数据（4×H100, DP=2 over TP=2）**：

| 指标 | vs SGLang 最强 baseline | vs vLLM 最强 baseline |
|:-----|:----------------------|:---------------------|
| Cache Hit | **3.0×** | **2.6×** |
| TTFT | **7.9×** | **7.1×** |
| E2E Latency | **6.7×** | **5.5×** |
| Throughput | **3.6×** | **4.5×** |

> 在无可利用前缀结构的 workload 上，噪声级别内匹配 baseline

---

### 🥇 SAC: CXL Disaggregated KV Cache for Sparse Attention（cs.DC, Jun 17）

> arXiv:2606.19746 · DeepSeek-V3.2 + SGLang

**核心创新**：首个针对稀疏 attention 模型优化的 CXL 解耦 KV cache 系统。利用 CXL 缓存行级 load/store 语义，按需只取 top-k KV 条目。

**性能数据**：

| 指标 | SAC vs RDMA baseline |
|:-----|:--------------------|
| Throughput | **2.1×** |
| TTFT | **9.7× 更低** |
| TBT (Token-Bound Time) | **1.8× 更低** |

> 标志着 CXL 作为稀疏 attention 模型解耦基础设施的可行性验证

---

### 🥇 STAR-KV: Low-Rank KV Cache via Soft Thresholding（cs.LG, Jun 6）

> arXiv:2606.08382 · Triton custom kernels · GitHub 开源

**核心创新**：可微分阈值自适应 rank 选择（attention head + block 级）+ 混合低秩分解（K/V 不同策略）+ 低秩感知混合精度量化。

**性能数据**：
- **75% KV 缓存压缩**（仅低秩）
- 叠加量化 → **20× 总体缩减**
- Triton kernel → **6.9× attention 模块加速**
- **3.1× 端到端生成吞吐**

---

### 🥇 OScaR: INT2 KV Cache Quantization（cs.LG, May 19）

> arXiv:2605.19660 · Canalized Rotation + Omni-Token Scaling

**核心创新**：发现 Token Norm Imbalance (TNI) 是量化保真度的主要瓶颈。提出 Canalized Rotation 消除 TNI + Omni-Token Scaling 处理 sequence 维度方差。

**性能数据（vs BF16 FlashDecoding-v2）**：
- **3.0× decode speedup**
- **5.3× 显存降低**
- **4.1× throughput 提升**
- INT2 下 near-lossless

---

### 🥇 PolyQ: Fractional-Bit CPU Quantization（ICCAD'26, cs.LG/AR, Jul 17）

> arXiv:2607.14618 · CPU-only LLM 推理

**核心创新**：activation-aware channel-wise bit allocation，每 channel 从 {2,3,4,8,16} 中选 → permute+cluster 为 bit-homogeneous blocks → 生成 SIMD/LUT kernel。

**性能数据（Falcon-H1-3B, Llama2-13B, Qwen3-32B）**：
- PPL 改善 **2.4–32.1%** vs 已有方法（3b 目标）
- 布局重排降低 activation 重排流量 **70.8%**
- energy/token 额外开销 < **2%**
- 在 workstation/laptop/mobile 三类 CPU 上验证

---

### 🥇 ExaGEMM: CPU Low-Bit GEMM 探索框架（ICCAD'26, cs.AR, Jul 17）

> arXiv:2607.14622 · register-resident LUT execution

**核心创新**：利用现有 SIMD datapath 做 table generation + accumulation，新增硬件仅为 in-register select/feed 机制。候选空间剪枝 99.2%。

**性能数据**：
- **13.29× latency speedup** over software-only baselines
- 跨多种精度（1/2/4-bit weights）+ ML 模型验证

---

### 🥇 HMA-Serve: Cross-Vendor Disaggregated Serving（cs.AR, Jun 29）

> arXiv:2606.29986 · GDDR prefill + HBM decode

**核心创新**：跨厂商 GDDR 加速器（预填充）+ HBM GPU（解码）混搭——phase-wise 量化 + compute-transfer pipeline + 延迟反量化。

**性能数据（4× Qwen3 4B–32B, 3 production traces）**：
- **3.2×** goodput vs SOTA memory-homogeneous 方法
- **4.8×** goodput-per-dollar
- 生成质量零损失

---

### 🥇 SwiftCache: Cross-Model NVLink KV Sharing（cs.DC, Jun 14）

> arXiv:2606.16135 · 多模型共享空闲 GPU 内存 + NVLink

**核心创新**：低 KV cache 需求的模型捐赠空闲 GPU 内存给高需求模型的 prefix cache → 通过 NVLink 跨模型共享。每层只在本地 GPU 保留 active layer 的 KV cache。

**性能数据**：
- **P99 TTFT 降低 69%**
- **最大上下文长度延长 3.98×**（vs vLLM/SGLang）
- 对同驻模型的干扰极小

---

### 🥇 TriAxialKV: Mixed-Precision KV for Agent Workloads（cs.LG, May 16）

> arXiv:2605.17170 · 三轴（temporal/modality/role）tag + 校准

**核心创新**：为每 token 分配三轴标签（时间近现性、模态、语义角色），校准 per-tag 灵敏度 → 在固定预算下分配 INT2/INT4。

**性能数据（Qwen3-VL-32B-Thinking + OSWorld）**：
- 匹配 SGLang BF16 KV cache 精度
- **4.5× KV 缓存大小**
- **30% 更高端到端吞吐**

---

### 🥇 Lynx: Progressive Speculative KV Quantization（cs.DC, Jul 2）

> arXiv:2607.01831 · 解耦推理中 KV 传输加速

**核心创新**：KV cache 按位重要性分 Anchor（高位）/Residual（低位）流 → Anchor 到达即开始推测解码，Residual 同时传输后验证。

**性能数据**：
- TTFT **1.43×** 优于标准 8-bit KV 量化
- 精度匹配 BF16，比 SOTA **高 5.1% 精度**

---

### 🥇 LUMEN: Coordinated Failure Recovery（cs.DC, Jun 16）

> arXiv:2606.17787 · 三点恢复决策框架

**核心创新**：LLM serving 故障恢复视为负载感知协同问题——检查点放置（故障前）、中断请求分配（故障时）、容量恢复（模型重载期间）。

**关键设计**：
- 幸存 worker 不从头重跑中断请求
- 检查点利用固定邻居而非全部重算
- 模型重载期间利用存活 worker 吸收流量

---

### 🥇 MoE-nD: Per-Layer KV Compression Routing（cs.LG, Apr 19）

> arXiv:2604.17695 · Mixture-of-Experts for 压缩策略选型

**核心创新**：每层路由到各自的 (eviction-ratio, K-bits, V-bits) 三元组，greedy solver 离线校准。

**性能数据（LongBench-v1 16k, 4 tasks）**：
- **14× 压缩**（1.9 GB → 136 MB）匹配未压缩 baseline
- AIME 推理 +**6–27 pts** over 最强 per-layer-quantization baseline

---

## 三、值得关注的其他论文

| 论文 | 方向 | 关键数据 | 来源 |
|:-----|:-----|:---------|:-----|
| **KARA / KvLLM**（cs.CL, May→Jul 2） | 推理模型滑动窗口 KV 压缩 | 解码态压缩 + vLLM 集成 | 2607.01237 |
| **BlockServe**（cs.LG, Jul 9） | dLLM 块粒度连续批处理 | **1.9–10.6×** throughput over Fast-dLLM | 2607.08930 |
| **MiMo-V2.5 全流水线优化**（cs.AR, Jul 13） | Hybrid SWA + MoE + 多模态 | O(W) KV 存储 + GCache RDMA 基础设施 | 2607.13095 |
| **Lynx**（cs.DC, Jul 2） | 渐进推测 KV 量化 | TTFT **1.43×**, 精度+**5.1%** | 2607.01831 |
| **KernelSight-LM**（cs.PF, Jun 26→Jul 2） | Kernel 级 LLM 推理模拟器 | p50 误差 **3.0%**（TTFT） | 2606.28565 |
| **HMA-Serve**（cs.AR, Jun 29） | MemHA 跨厂商解耦 | **3.2×** goodput, **4.8×** goodput-per-dollar | 2606.29986 |
| **Concordia**（cs.DC, Jun 22） | JIT 编译持久 kernel 检查点 | 设备驻留 delta-checkpoint | 2606.23521 |
| **Geometric Scheduling（SVF）**（cs.AI, Jun 21→24） | 几何感知在线调度 | 竞争比 **3**（高并发），vLLM 集成 | 2606.22327 |
| **HERALD**（cs.LG, Jun 19） | dLLM CPU-GPU 协同 KV 检索 | **1.59×** per-block, **2.47×** throughput | 2606.21633 |
| **ReMP**（cs.DC, Jun 17） | 运行时模型并行度重配置 | **1–7s** 拓扑切换 vs 数分钟重启 | 2606.18741 |
| **VeriAttn**（cs.LG, Jun 15） | 通信高效可验证 attention | **2.60–5.42×** vs TSDP | 2606.16352 |
| **Bifrost TEE-FHE 混合推理**（cs.CR, Jun 15） | 隐私保护推理 | **9.25–9.91×** latency 改善 | 2606.17421 |
| **HyperQuant**（cs.LG, Jun 22） | 率失真最优量化流水线 | H100 4bps 权重 3.9× + KV 3.79× near-lossless | 2606.23406 |
| **ELiTeFormer FPGA**（cs.AR, Jul 3） | 线性 attention + ternary | 10× weight, 12.8× KV 压缩, **3.9×** vs A100 | 2607.03652 |
| **VQKV**（cs.CL, Mar 17） | 训练-free 向量量化 | **82.8%** 压缩率, **98.6%** baseline 性能 | 2603.16435 |
| **RDKV**（cs.LG, May 8） | 率失真联合驱逐+量化 | **97.81%** acc @ **2.48%** 缓存, **4.5×** decode | 2605.08317 |

---

## 四、趋势判断

1. **KV Cache 压缩收敛为系统工程**：OScaR/STAR-KV/HyperQuant 将低秩 + 量化 + 旋转变换统一到端到端 serving 系统中，不再孤立优化
2. **CPU 推理重获关注**：PolyQ（ICCAD'26）和 ExaGEMM（ICCAD'26）展示了 CPU LLM 推理在边缘/无 GPU 场景的可行性，PPL 改善高达 32.1%
3. **解耦推理路线分化**：CXL（SAC）vs RDMA（传统）vs 跨厂商（HMA-Serve），系统架构选择出现多样性
4. **PyTorch 2.13 加固编译器基础设施**：CuTeDSL Native DSL 开辟 Triton 之外的第二 GPU 编译路径，`nn.LinearCrossEntropyLoss` 解决大词表显存痛点
5. **推理框架支持深度扩展**：TRT-LLM DSV4 支持、SGLang Decode Context Parallelism、vLLM 的通用推测解码，框架功能集在快速收敛

---

*报告由 AI 自动化生成，数据截止 2026-07-18 02:45 UTC+8。具体数据请以原始来源为准。*
